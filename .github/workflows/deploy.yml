name: Deploy MkDocs to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        os: [ubuntu-24.04]
        python-version: [3.10]
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          pip install mkdocs-material mkdocs-glightbox pymdown-extensions mkdocs-macros-plugin

      - name: Deploy to GitHub Pages
        run: |
          mkdocs gh-deploy --force -c
